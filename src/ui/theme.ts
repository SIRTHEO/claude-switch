// src/ui/theme.ts
// Shared visual constants for the Ink UI. Kept tiny on purpose — the brand
// orange is the single colour the dashboard owns; everything else falls back
// to Ink's named palette so terminal themes can override.

export const ORANGE = '#D97757';
